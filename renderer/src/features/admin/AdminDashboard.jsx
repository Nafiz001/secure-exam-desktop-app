export default function AdminDashboard() {
  return (
    <section className="card">
      <h2>Admin Panel</h2>
      <p className="muted">
        Admin panel is still pending implementation in both backend feature scope and frontend UX.
      </p>
    </section>
  );
}
